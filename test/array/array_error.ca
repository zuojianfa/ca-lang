fn main() {
    let a: [f32; 2] = [1.1f64, 2.2f64]; // should cannot compiled
    let b: [f64; 2] = [1.1f32, 2.2f32]; // should cannot compiled
    //let c: [f64; 2] = [1.1, 2.2]; // should cannot compiled
}

